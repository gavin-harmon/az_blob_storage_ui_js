{
  "dependencies": {
    "shadcn-ui": "^0.9.4"
  },
  "scripts": {
    "build": "pip install -r requirements.txt && cd frontend && npm install && npm run build",
    "start": "cd api && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
